meta {
  name: Login
  type: http
  seq: NaN
}

post {
  url: http://localhost:3000/login
  body: json
  auth: inherit
}

body:json {
  {
    "username":"byga",
    "password":"q1w2e3r4t5y6"
  }
}

script:post-response {
  if(res.body){
    bru.setVar("jwt_token",res.body)
  }
}

settings {
  encodeUrl: true
}
