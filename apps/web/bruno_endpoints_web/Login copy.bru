meta {
  name: Login copy
  type: http
  seq: 3
}

get {
  url: http://localhost:3001/
  body: json
  auth: inherit
}

script:post-response {
  if(res.body){
    bru.setVar("jwt_token",res.body)
  }
}

settings {
  encodeUrl: true
}
